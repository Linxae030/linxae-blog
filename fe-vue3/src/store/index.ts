import { createStore } from 'vuex'

export default createStore({
  state: {
    //控制footer是否展示(主要用在首屏不展示)
    isShowFooter: true, //默认值：true
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
